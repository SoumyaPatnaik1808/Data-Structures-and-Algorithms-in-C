#include <stdio.h> 

int main(){
    int arr[5] = {1, 2, 3, 4, 5}; // declaration and initialization of an array
    printf("The first element of the array is: %d\n", arr[0]); // accessing the first element of the array
    printf("The second element of the array is: %d\n", arr[1]); // accessing the second element of the array
    printf("The third element of the array is: %d\n", arr[2]); // accessing the third element of the array
    printf("The fourth element of the array is: %d\n", arr[3]); // accessing the fourth element of the array
    printf("The fifth element of the array is: %d\n", arr[4]); // accessing the fifth element of the array

    return 0;

    // size of array : 0 to n-1
}